import { baseColors } from "../../theme/colors";

export type ToggleTheme = {
  handleBackground: string;
};

export const light: ToggleTheme = {
  handleBackground: baseColors.wigoWhite,
};

export const dark: ToggleTheme = {
  handleBackground: baseColors.wigoWhite,
};
